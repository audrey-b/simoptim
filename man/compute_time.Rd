% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_time.R
\name{compute_time}
\alias{compute_time}
\title{Compute total estimated compute time}
\usage{
compute_time(r, m, burnin_time, eff_sample_time, n_cores = 1)
}
\arguments{
\item{r}{A positive integer giving the number of simulation datasets.}

\item{m}{A positive integer giving the number of effective posterior
samples per dataset.}

\item{burnin_time}{Average burn-in time per MCMC run (in any consistent
time unit, e.g. seconds).}

\item{eff_sample_time}{Average time to generate one effective posterior
sample after burn-in (same units as \code{time_burnin}).}

\item{n_cores}{A positive integer giving the number of cores available for
parallel execution. Defaults to \code{1} (no parallelisation).}
}
\value{
A single positive numeric value giving the total estimated compute
  time, calculated as \eqn{r \cdot (t_{\text{burnin}} + m \cdot
  t_{\text{per eff sample}})}.
}
\description{
Computes the total estimated compute time for a simulation study given
an allocation of datasets and effective posterior samples.
}
\examples{
# Single core
compute_time(r = 100, m = 20, burnin_time = 10, eff_sample_time = 1)

# With 4 cores
compute_time(r = 100, m = 20, burnin_time = 10, eff_sample_time = 1,
             n_cores = 4)
}
\seealso{
\code{\link{optimize_allocation}},
  \code{\link{optimize_allocation_from_ratios}}
}
